<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light">
        <!-- Customize your theme here. -->
    </style>

    <style name="weibo_tab">
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_weight">1</item>
        <item name="android:gravity">center</item>
        <item name="android:textSize">14sp</item>
        <item name="android:paddingTop">8dip</item>
        <item name="android:paddingBottom">4dip</item>
        <item name="android:background">@drawable/weibolist_bottombar_itembg_selector</item>
        <item name="android:textColor">@color/weibo_listtab_off</item>
        <item name="android:button">@null</item>
    </style>

    <style name="AnimationFade">

        <!-- PopupWindow左右弹出的效果 -->
        <item name="android:windowEnterAnimation">@anim/popdown</item>
        <item name="android:windowExitAnimation">@anim/popup</item>
    </style>

    <style name="PersonBookAnimationFade">

        <!-- PopupWindow左右弹出的效果 -->
        <item name="android:windowEnterAnimation">@anim/personbook_popdown</item>
        <item name="android:windowExitAnimation">@anim/personbook_popup</item>
    </style>

</resources>